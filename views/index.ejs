<!DOCTYPE html>
<html>
  <head>
    <title>Small preview</title>
    <link rel='stylesheet' href='/stylesheets/normalize.css' />
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="http://code.jquery.com/jquery-2.1.4.min.js"></script>
    <script src="http://code.jquery.com/ui/1.11.4/jquery-ui.min.js"></script>
    <link type="text/css" href="/jquery-ui/jquery-ui.min.css" rel="stylesheet" />
    <link type="text/css" href="/jquery-ui/jquery-ui.structure.min.css" rel="stylesheet" />
    <link type="text/css" href="/jquery-ui/jquery-ui.theme.min.css" rel="stylesheet" />
    <script src="/jquery-ui/jquery-ui.min.js" type="text/javascript"></script>

    <link href='https://fonts.googleapis.com/css?family=Play:400,700&subset=latin,cyrillic' rel='stylesheet' type='text/css'>

    <meta charset="utf-8">
  </head>
  <body>
   <div class="wrapper">
    <header>
      <div class="logo"><a href="/"><img src="/images/logo.png" alt=""></a></div>
      <div class="maintext">
        <h1>Гуртожиток ХАІ-11</h1>
        <h2>Струдентська рада</h2>
        <p>Таблиця порушників правил і положень гуртожитку</p>
      </div>
      <div class="log">
        <% if (isLogged != 1) {%>
        <form action="" method="post">
          <input type="text" placeholder="Логин" name="login" id="login">
          <input type="password" placeholder="Пароль" name="password" id="password">
          <input type="submit" value="Подтвердить" id="submit">
        </form>
        <%} else if (isLogged == 1){%>
        <form action="" method="post">
          <input type="button" value="Вийти" id="logout">
        </form>
        <% } %>
      </div>
    </header>
    <section class="offender">
      <div class="head">
        <div class="name">Прізвище Ім’я
          <button id="nmdown"></button>
          <button id="nmup"></button>
        </div>
        <div class="num">Гос. від-ки
          <button id="nmbdown"></button>
          <button id="nmbup"></button>
        </div>
        <div class="block">Блок
          <button id="bdown"></button>
          <button id="bup"></button>
        </div>
        <div class="room">Кімната</div>
        <div class="hdate">Дата
          <button id="ddown"></button>
          <button id="dup"></button>
        </div>
        <div class="off">Порушення</div>
      </div>
      <% if (isLogged == 1){%>
      <div class="row">
        <textarea></textarea>
        <textarea placeholder="Прізвище та ім’я"></textarea>
        <textarea placeholder="Кількість відробіток"></textarea>
        <textarea placeholder="Блок"></textarea>
        <textarea placeholder="Кімната"></textarea>
        <input type="text" class="datepicker" placeholder="Дата порушення">
        <textarea placeholder="Порушення"></textarea>
        <div class="actions">
          <button class="apply">Додати</button>
        </div>
      </div>
      <% } %>
      <% for (key in list) {%>
        <div class="row">
        <% for (key1 in list[key]) {%>
          <% if (isLogged != 1) {%>
            <% if (key1 == 'date' || key1 == 5) { %>
          <script>
            function getDate(srt) {
              var date = new Date(srt),
                      mm = date.getMonth()+1,
                      dd = date.getDate(),
                      yyyy = date.getFullYear();
              return mm+'/'+dd+'/'+yyyy;
            }
            var x = document.createElement('p');
            x.textContent = getDate('<%= list[key][key1]%>');
            x.setAttribute('class', 'date');
            var closestAct = $('div').last('.row');
            $(x).appendTo(closestAct);
          </script>
            <% } else { %>
              <p><%= list[key][key1]%></p>
            <% } %>
          <%} else if (isLogged == 1){%>
            <%if (key1 == 'date' || key1 == 5) { %>
            <script>
              function getDate(srt) {
                var date = new Date(srt),
                        mm = date.getMonth()+1,
                        dd = date.getDate(),
                        yyyy = date.getFullYear();
                return mm+'/'+dd+'/'+yyyy;
              }
              var x = document.createElement("INPUT");
              x.setAttribute("type", "text");
              x.setAttribute("class", "datepicker");
              x.setAttribute("value", getDate('<%-list[key][key1]%>'));
              var closestAct = $('div').last('.row');
              $(x).appendTo(closestAct);
            </script>
            <% continue;}%>
            <textarea><%= list[key][key1] %></textarea>
          <% } %>

        <% } %>
          <% if (isLogged == 1){%>
          <div class="actions">
            <button class="edit">Змінити</button>
            <button class="remove">Видалити</button>
          </div>
          <% } %>
        </div>

      <% } %>
    </section>
    <footer>
      <div class="contacts">
        <div class="tel">
          <p>Зателефонувати:</p>
          <ul>
            <li>+38 (055) 55 55 555 - Lorem ipsum</li>
            <li>+38 (055) 55 55 555 - Lorem ipsum</li>
            <li>+38 (055) 55 55 555 - Lorem ipsum</li>
          </ul>
        </div>
        <div class="soc">
          <p>Написати в VK:</p>
          <ul>
            <li class="vk"><a href="#">Lorem ipsum</a></li>
            <li class="vk"><a href="#">Lorem ipsum</a></li>
            <li class="vk"><a href="#">Lorem ipsum</a></li>
          </ul>
        </div>
      </div>
      <div class="clear"></div>
      <div class="author">
        <p>&copy TeaDrinker 2015</p>
      </div>
    </footer>
    <script>
      $('.actions').click(function(e) {
        var target = e.target;
        var closestrow = $(target).closest('.row');
        var id = $(closestrow).find('textarea')[0];
        var fullName;
        fullName = $(closestrow).find('textarea')[1];
        var number = $(closestrow).find('textarea')[2];
        var block = $(closestrow).find('textarea')[3];
        var room = $(closestrow).find('textarea')[4];
        var date = $(closestrow).find('input')[0];
        var offender = $(closestrow).find('textarea')[5];
        id = $(id).val();
        fullName = $(fullName).val();
        number = $(number).val();
        block = $(block).val();
        room = $(room).val();
        date = new Date($(date).val());
        date = date.toISOString();
        offender = $(offender).val();
//        console.log(id + ' ' + fullName + ' ' + number
//                + ' ' + ' ' + block + ' ' + room + ' '
//                + date + ' ' + offender);
        if (target.className == 'edit') {
          $.ajax({
            url: '/update',
            type: 'POST',
            cache: false,
            data: {id: id, fullName: fullName, number: number,
              block: block, room: room, date: date, offender: offender},
            success: function(data){
              window.alert('Success!');
            },
            error: function(jqXHR, textStatus, err){
              console.log('text status '+textStatus+', err '+err)
//              window.location.reload();
            }
          });
        }
        if (target.className == 'remove') {
          $.ajax({
            url: '/remove',
            type: 'POST',
            cache: false,
            data: {id: id},
            success: function(data){
              window.alert('Success!');
            },
            error: function(jqXHR, textStatus, err){
              console.log('text status '+textStatus+', err '+err)
//              window.location.reload();
            }
          });
        }
        if (target.className == 'apply') {
          fullName = $(closestrow).find('textarea')[1];
          number = $(closestrow).find('textarea')[2];
          block = $(closestrow).find('textarea')[3];
          room = $(closestrow).find('textarea')[4];
          date = $(closestrow).find('input')[0];
          offender = $(closestrow).find('textarea')[5];
          fullName = $(fullName).val();
          number = $(number).val();
          block = $(block).val();
          room = $(room).val();
          date = new Date($(date).val());
          date = date.toString();
          offender = $(offender).val();
          $.ajax({
            url: '/insert',
            type: 'POST',
            cache: false,
            data: {fullName: fullName, number: number,
              block: block, room: room, date: date, offender: offender},
            success: function(data){
              window.alert('Success!');
            },
            error: function(jqXHR, textStatus, err){
              console.log('text status '+textStatus+', err '+err)
//              window.location.reload();
            }
          });
        }
        window.location.reload();
      });
      $(function(){
        $.datepicker.setDefaults(
                $.extend( $.datepicker.regional[ '' ] )
        );
        $( '.datepicker' ).datepicker();
      });
      function setCookie(name, value, options) {
        options = options || {};

        var expires = options.expires;

        if (typeof expires == "number" && expires) {
          var d = new Date();
          d.setTime(d.getTime() + expires * 1000);
          expires = options.expires = d;
        }
        if (expires && expires.toUTCString) {
          options.expires = expires.toUTCString();
        }
        value = encodeURIComponent(value);

        var updatedCookie = name + "=" + value;

        for (var propName in options) {
          updatedCookie += "; " + propName;
          var propValue = options[propName];
          if (propValue !== true) {
            updatedCookie += "=" + propValue;
          }
        }
        document.cookie = updatedCookie;
      }
      $('#nmdown').click(function() {
        window.location = 'sortnamedown';
      });
      $('#nmup').click(function() {
        window.location = 'sortnameup';
      });
      $('#nmbdown').click(function() {
        window.location = 'sortbynumberdown';
      });
      $('#nmbup').click(function() {
        window.location = 'sortbynumberup';
      });
      $('#bdown').click(function() {
        window.location = 'sortbyblockdown';
      });
      $('#bup').click(function() {
        window.location = 'sortbyblockrup';
      });
      $('#ddown').click(function() {
        window.location = 'sortbydatedown';
      });
      $('#dup').click(function() {
        window.location = 'sortbydateup';
      });


      $('#submit').click(function() {
        $.ajax({
          url: '/',
          type: 'POST',
          cache: false,
          data: { login: $('#login').val(), password: $('#password').val() },
          success: function(data){
//            alert('Success!');
            $('#login').val('');
            $('#password').val('');
            window.location.reload();
            window.location.reload();
            window.location.reload();
          },
          error: function(jqXHR, textStatus, err){
            console.log('text status '+textStatus+', err '+err)
            window.location.reload();
          }
        });
        window.location.reload();
      });
      $('#logout').click(function() {
        $.ajax({
          url: '/logout',
          type: 'POST',
          cache: false,
          success: function () {
            window.location.reload();
            window.location.reload();
            window.location.reload();
          },
          error: function () {
//            alert('NO!')
          }
        });
      });
    </script>
   </div>
  </body>
</html>
